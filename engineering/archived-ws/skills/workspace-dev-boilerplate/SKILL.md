---
name: workspace-dev-boilerplate
description: >-
  Creates the four-file workspace shell: .env, Procfile, Makefile, and
  *.code-workspace. Use when setting up a multi-repo dev workspace, wiring
  Overmind, or copying archived-ws orchestration patterns. Does not handle
  git clone — user controls repo layout.
---

# Workspace Dev Boilerplate (Cursor)

Follow the agent-agnostic guide: **[GUIDE.md](GUIDE.md)**

Core pattern: **`.env` + `Procfile` + `Makefile` + `*.code-workspace`**
