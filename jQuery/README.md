# jQuery-playlist
A playlist for jQuery

# How to play 

Serve the current directory as an http server to look at the htmls.

```bash
# install http-server
npm i / yarn

# run the server
npm run serve
```
