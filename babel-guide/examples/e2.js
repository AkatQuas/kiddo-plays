'use strict';

const number = 1;

/**
 * @param {number} x
 * @param {number} y
 */
const add = (x, y) => x + y;

/**
 * @param {number} s
 * @param {number} t
 */
function subtract(s, t) {
  return s - t;
}
