# TinyLlama

Tinyllama server, originial is `ghcr.io/huggingface/tinyllama-server:cpu`.


```bash
# start up the llm service
docker compose up

# test completion api and wait for response
./verify.sh
```

