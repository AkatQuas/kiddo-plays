# Hugging Face mirror (recommended for CN users)
HF_ENDPOINT=https://hf-mirror.com
# Disable XET protocol (mirror cannot proxy cas-server.xethub.hf.co)
HF_HUB_DISABLE_XET=1

# Model settings
MODEL_ID=openai/clip-vit-large-patch14
MODEL_PATH=models/clip-vit-large-patch14
GALLERY_PATH=data/gallery

# Server settings
HOST=0.0.0.0
PORT=11204
