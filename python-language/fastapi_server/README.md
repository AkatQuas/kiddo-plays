# FastAPI server

Create and activate a virtual environment and then install dependencies. **`uv venv`**.

```bash
uv pip install -r requirements.txt
```

This project is a basic playground for [fastAPI](https://fastapi.tiangolo.com/tutorial/).

Start the server using,

```bash
fastapi dev main.py
```

Try the request in [test.http](./test.http).
